name: Build and Deploy Code

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
      - name: pulling git repo
        uses: actions/checkout@v5

      - name: Greet Sam
        run: echo "Peace Sam"